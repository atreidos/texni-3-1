# QA — краткий журнал проверок

## N1

1. Тест «Пользователь регистрируется и попадает в личный кабинет» (`01-регистрация-и-редирект-в-личный-кабинет`) упал.
2. Ошибка: ожидался URL `/dashboard`, фактически остались на `/auth/register` (таймаут 15 с). Вероятно подтверждение email в Supabase или лимит регистраций, не связано с подключением Sentry.
3. исправил: не применялось (изменён только Sentry).
4. Скриншот: `frontend/test-results/01-регистрация-и-редирект--e0fbe-и-попадает-в-личный-кабинет-chromium/test-failed-1.png`

---

## N2

1. Тест «структурированное логирование Edge» — автоматизированный Playwright не покрывает stdout функций в облаке.
2. Проверка: статический обзор кода — все функции обёрнуты в `runWithHttpMutationLog`, `console.error` заменён на `logError` где был; `deno` в PATH на машине агента отсутствует, `deno check` не запускался.
3. Для ручной проверки после деплоя: вызвать любую мутацию (например `organizations-list` с POST) и в логах функции в Dashboard убедиться в строках JSON с `type":"http_mutation"`; при `EDGE_STRUCTURED_LOGGING=false` новых строк из модуля быть не должно.
4. Скриншот: нет

---

## Сводка 26.03.2026 (после Sentry)

- `npm run build` — успех.
- Playwright: 17 passed, 1 failed (см. N1).
- Проверки вручную: `npm run dev`, главная `/`, панель внизу справа «Отправить тест в Sentry» → в Sentry **Issues**, environment `development`, текст ошибки `DocFlow: ручная проверка Sentry`.
